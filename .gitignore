# The directory Mix will write compiled artifacts to.
/_build/

# If you run "mix test --cover", coverage assets end up here.
/cover/

# The directory Mix downloads your dependencies sources to.
/deps/

# Where third-party dependencies like ExDoc output generated docs.
/doc/

# Ignore .fetch files in case you like to edit your project deps locally.
/.fetch

# If the VM crashes, it generates a dump, let's ignore it too.
erl_crash.dump

# Also ignore archive artifacts (built via "mix archive.build").
*.ez

# Ignore package tarball (built via "mix hex.build").
fnord-*.tar

# Temporary files, for example, from tests.
/tmp/

# ElixirLS artifacts
.elixir_ls

# escript
/fnord

# Temporary prototype files
.scratch/
scratch*
scratch.tar.gz
tags

# JetBrain's IDE local configuration files
.idea/

# Why did Apple think this was a solid design choice?
.DS_Store

# Fnord generates backups when it writes code. Ignore them and use `make clean-bak` to remove them.
**/*.*.bak

# We aren't *quite* good enough to bootstrap our own compilation, so to speak :)
.claude
CLAUDE.local.md

# Notes and plans
.notes

# excoveralls
cover/
*.coverdata

# fnord-generated planning docs
.fnord/*
